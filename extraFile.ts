// export const userLoginResponse = {
//     "_id": expect.any(String),
//     "name": "vamshi krishna",
//     "email": "Vamshi@someone.com",
//     "accessToken": expect.any(String),
//     "refreshToken": expect.any(String)
// }
// const encryptedText = CryptoJS.AES.encrypt('data@1234', 'my-secret-key@123',).toString();
        // console.log('encryptedText ', encryptedText, 'originalEnterPassword ', password);

        // const decryptedText = await CryptoJS.AES.decrypt(password, 'my-secret-key@123')
        // const passwordDecrypted = decryptedText.toString(CryptoJS.enc.Utf8).split('"');

        // const authenticate = await dbService.correctPassword(passwordDecrypted[1], oldPassword);

        // if (authenticate === false) {
        //     return false;
        // }
        // return true;
        // console.log('originalEnterPassword ', password, 'hashedPasswordFromTheDatabase', oldPassword);