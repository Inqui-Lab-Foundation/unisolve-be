name: Test-Build
on:
  push:
    branches:
      - postman_tests

jobs:
  test-api:
    runs-on: ubuntu-latest
  steps:
    - name: Checkout
      uses: actions/checkout@v1

    - name: Run local k6 test
      uses: grafana/k6-action@v0.2.0
      with:
        filename: k6-token.js
